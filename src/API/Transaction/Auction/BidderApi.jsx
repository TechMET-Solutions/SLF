import axios from "axios";
import { encryptData, decryptData } from "../../../utils/cryptoHelper";
import { API } from "../../../api";

const API_BASE = `${API}/Transaction/Auction`;


export const fetchBidderApi = async (page = 1, limit = 10) => {
    try {
        
    } catch (error) {
        
    }
}

export const registerBidderApi = async (payload) => {
  try {
        
    } catch (error) {
        
    }
}
export const updateBidderApi = async (payload) => {
  try {
        
    } catch (error) {
        
    }
}
export const viewBidderApi = async (payload) => {
  try {
        
    } catch (error) {
        
    }
}